import { environment } from '@environments/environment';

export const discountCodes = environment.DISCOUNT_CODES;
export const shippingOptions = environment.SHIPPING_OPTIONS;
export const maxOrderQty = environment.MAX_ORDER_QTY;